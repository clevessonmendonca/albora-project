/** PT type aliases — compatibilidade com código legado. */
export type { Queue as Fila } from "./fila";

/** English alias — preferred for new code. */
export { comEvento as withEvent } from "./event";
