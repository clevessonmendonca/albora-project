/**
 * `@albora/integrations` — a fronteira externa (ADR 0016). Só billing mora
 * aqui por enquanto; e-mail e storage seguem em `apps/web/lib` até a próxima
 * migração desta dívida.
 */
export * from "./billing";
