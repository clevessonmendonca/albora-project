/**
 * Primitivas web. Só desenham.
 *
 * Nenhum hex literal, nenhuma classe de cor arbitrária — o guard `tokens`
 * reprova. Nenhuma regra de negócio, validação ou chamada de API — o guard
 * `dominio` reprova. O que não desenha pixel mora em `@albora/core`, para
 * poder ser reaproveitado pelo app Expo sem refactor (ADR 0010).
 */
export { Botao } from "./Botao";
