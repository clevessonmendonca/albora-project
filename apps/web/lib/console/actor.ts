/** Barrel: T10 (login) e T14 (shell do console) importam Actor e resolveActor daqui, sem conhecer staff-session.ts. */
export type { Actor } from "@albora/core";
export { resolveActor } from "./staff-session";
