# MUDANÇAS DE ACESSIBILIDADE A APLICAR

## NOVOS ARQUIVOS

### 1. `/workspace/packages/ui-web/src/live-announcer.tsx`
```typescript
"use client";

import { useEffect, useRef, useState } from "react";

const announceQueue: string[] = [];
const listeners = new Set<() => void>();

export function announce(message: string) {
  announceQueue.push(message);
  listeners.forEach((fn) => fn());
}

export function LiveAnnouncer() {
  const [current, setCurrent] = useState("");
  const timeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null);

  useEffect(() => {
    const handler = () => {
      if (announceQueue.length === 0) return;
      const msg = announceQueue.shift();
      if (msg) {
        setCurrent(msg);
        if (timeoutRef.current) clearTimeout(timeoutRef.current);
        timeoutRef.current = setTimeout(() => setCurrent(""), 100);
      }
    };

    listeners.add(handler);
    return () => {
      listeners.delete(handler);
      if (timeoutRef.current) clearTimeout(timeoutRef.current);
    };
  }, []);

  return (
    <div
      role="status"
      aria-live="polite"
      aria-atomic="true"
      className="sr-only"
    >
      {current}
    </div>
  );
}
```

### 2. `/workspace/packages/ui-web/src/skip-link.tsx`
```typescript
export function SkipLink({ href = "#main-content" }: { href?: string }) {
  return (
    <a
      href={href}
      className="sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-50 focus:rounded-pilula focus:bg-acento focus:px-4 focus:py-2 focus:text-sobre-acento focus:no-underline focus:shadow-e2 focus:outline-none focus:ring-2 focus:ring-acento focus:ring-offset-2"
    >
      Ir para o conteúdo
    </a>
  );
}
```

### 3. `/workspace/.a11y-audit.md`
(Já criado - ver arquivo)

## MUDANÇAS EM ARQUIVOS EXISTENTES

### `packages/ui-web/src/index.ts`
Adicionar exports:
```typescript
export { LiveAnnouncer, announce } from "./live-announcer";
export { SkipLink } from "./skip-link";
```

### `packages/ui-web/src/guest-chrome.tsx`
Converter GuestMain para `<main>`:
- Trocar `<div` por `<main id="main-content"`
- Fechar com `</main>`

### `packages/ui-web/src/star.tsx`
Adicionar props animating e className:
```typescript
export function Star({ 
  size = 24, 
  filled,
  animating,
  className,
}: { 
  size?: number; 
  filled?: boolean;
  animating?: boolean;
  className?: string;
}) {
  return (
    <svg 
      width={size} 
      height={size} 
      viewBox="0 0 24 24" 
      aria-hidden="true"
      className={className}
      style={animating ? { animation: 'star-pop 500ms var(--curva)' } : undefined}
    >
      <style>{`
        @keyframes star-pop {
          0% { transform: scale(1); }
          50% { transform: scale(1.2); }
          100% { transform: scale(1); }
        }
        @media (prefers-reduced-motion: reduce) {
          svg { animation: none !important; }
        }
      `}</style>
      {/* ... resto do SVG ... */}
    </svg>
  );
}
```

### `apps/web/features/feed/components/ui/new-photos-button.tsx`
Adicionar aria-label:
```typescript
aria-label="Novas fotos disponíveis. Ir para o topo do feed"
```

### `apps/web/features/feed/components/ui/temporal-filter.tsx`
Na função FilterTab adicionar:
```typescript
aria-current={active ? "page" : undefined}
className com: "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-acento focus-visible:ring-offset-2 focus-visible:ring-offset-bg"
```

### `apps/web/features/feed/components/ui/feed-filter-panel.tsx`
Mesmas mudanças do temporal-filter

### `apps/web/features/feed/components/client/hour-strip.tsx`
Adicionar focus-visible no botão Circulo:
```typescript
className com: "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-acento focus-visible:ring-offset-2 focus-visible:ring-offset-bg rounded-md"
```

### `apps/web/features/feed/components/client/comment-sheet.tsx`
1. Adicionar import: `announce`
2. Adicionar label no input
3. Adicionar aria-label no submit button
4. Adicionar announce em publicarEScrollar
5. Adicionar announce em remover
6. Melhorar MenuOpcoes com focus trap e role="menu"

### `apps/web/features/feed/components/client/photo-interaction.tsx`
1. Import announce
2. Adicionar announce em alternarReacao
3. Adicionar announce em handleCompartilhar

### `apps/web/features/feed/components/client/feed-page.tsx`
1. Import LiveAnnouncer e SkipLink
2. Adicionar <SkipLink /> no topo
3. Adicionar <LiveAnnouncer />
4. Adicionar role="feed" e aria-busy em FeedColumn

### `apps/web/features/feed/components/client/viewer.tsx`
1. Adicionar aria-describedby="viewer-help"
2. Adicionar <p id="viewer-help"> com instruções de teclado
